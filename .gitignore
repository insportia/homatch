# Environment — NEVER commit
.env
.env.local
.env.*.local

# Supabase secrets directory
/supabase/secrets

# Logs
/logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
output
*.local
package-lock.json
official-worker/.tstest-build
playwright-report
test-results

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
package.json.[0-9]*
.sync
history/*.json
.vite_cache
/.skills

# tasks/ is persisted by MeDo but NOT pushed to GitHub
/tasks

# GitHub push config — contains personal access token, NEVER commit
/tasks/.github-config
.native-wind-cache
.vite
/historical_context.txt
/history
/.sync
.env
/supabase/secrets

# ── Local incident/debug artifacts (added 2026-09-08 cleanup) ──────────────
# Each production incident wrote its dumps to the repo root; ~30 such files
# had accumulated. None is an input to a build or a test.
/verify-forensic-*/
/browser-lifecycle-debug.txt
/live-job-debug.json
/latest-live-job-*.json
/patch-*.cjs
/push-homatch-fix-*.ps1
*.pre-consolidation
*.bak
*.before-*-fix

# Supabase CLI local link state (regenerated by the CLI; not secret, but not
# shared configuration either).
/supabase/.temp/
