# Communications Hub — what is finished and what is waiting

<!-- GENERATED by tests/matrix/databaseBlocked.test.mjs. Edit the matrix there, not this file. -->

16 features. 3 are complete and unblocked today. 12 are finished in code and wait only on the owner applying the migration. 2 additionally need something outside this repository.

No row below is waiting on code that has not been written.

| Feature | Status | What it waits on |
| --- | --- | --- |
| WhatsApp inbound webhook — signature check, dedupe, inbound record | IMPLEMENTED, LOCAL_UNIT_VERIFIED, BLOCKED_ONLY_BY_SCHEMA | comm_claim_webhook_event() and comm_record_inbound() do not exist until the migration is applied, so an end-to-end delivery cannot be replayed against a database. |
| WhatsApp outbound send — template gate, 24-hour service window | IMPLEMENTED, LOCAL_UNIT_VERIFIED, BLOCKED_ONLY_BY_SCHEMA | The window and template state are read from comm_conversations and comm_whatsapp_templates. |
| WhatsApp template sync from Meta | IMPLEMENTED, BLOCKED_ONLY_BY_SCHEMA, EXTERNAL | The synced rows land in comm_whatsapp_templates and the account they belong to in comm_channel_accounts; neither table exists until the migration is applied. ALSO: The production Meta access token is currently rejected with HTTP 401, so a live sync cannot be run even once the schema exists. |
| Campaign launch — audience summary, cost estimate, credit reservation | IMPLEMENTED, LOCAL_UNIT_VERIFIED, BLOCKED_ONLY_BY_SCHEMA | comm_audience_summary() and comm_enqueue_campaign() ship in the unapplied migration. The reservation half runs on the EXISTING wallet (beginExecution in _shared/billing.ts) and needs nothing new. |
| Dispatch worker — claim, send, settle, reclaim stale | IMPLEMENTED, LOCAL_UNIT_VERIFIED, BLOCKED_ONLY_BY_SCHEMA | comm_claim_sends() and comm_reclaim_stale_sends() ship in the unapplied migration. The rows themselves live in outreach_sends, which already exists. |
| Voice call lifecycle — provider webhook, transcript, cost settlement | IMPLEMENTED, LOCAL_UNIT_VERIFIED, BLOCKED_ONLY_BY_SCHEMA | The extraction is written to comm_extractions. The send row and the cost event both land in tables that already exist. |
| Realtime Georgian conversation — endpointing, barge-in, language lock | IMPLEMENTED, LOCAL_UNIT_VERIFIED, EXTERNAL | GE-VOICE-CORPUS-v1 measures every text-level decision. Acoustic STT accuracy on real telephony audio, Georgian TTS naturalness and true mouth-to-ear latency need a real call to a Georgian speaker. |
| Agent builder — draft, version, publish | IMPLEMENTED, BROWSER_VERIFIED, BLOCKED_ONLY_BY_SCHEMA | comm_publish_agent() writes the immutable version row into comm_agent_versions. |
| AI TALK homepage demo — allowance, session, transcript | IMPLEMENTED, LOCAL_UNIT_VERIFIED, BLOCKED_ONLY_BY_SCHEMA | The per-visitor allowance is counted in comm_talk_sessions. The admin switch and the limits themselves live in admin_settings, which already exists — so the ADMIN side of this feature is live today. |
| Compliance and risk engine — scoring, auto-pause, admin review | IMPLEMENTED, LOCAL_UNIT_VERIFIED, BLOCKED_ONLY_BY_SCHEMA | comm_compliance_pause() and the two tables it reads and writes. |
| Provider routing and kill switches | IMPLEMENTED, BROWSER_VERIFIED, BLOCKED_ONLY_BY_SCHEMA | comm_provider_routes, which holds the per-role priority, the enabled flag and the kill switch. The panel already degrades with a named reason rather than a blank card when that table is absent, which is the state a reader can see today. |
| Admin voice tuning and AI Talk allowance | IMPLEMENTED, BROWSER_VERIFIED | Nothing is blocked. Both write admin_settings, which exists in production, and the server code that reads them ships in the same branch. |
| Customer billing screen — balance, spend, breakdown, top-up | IMPLEMENTED, BROWSER_VERIFIED | Reads the EXISTING wallet and the existing outreach_sends rows. No second ledger was created, so nothing here waits on the migration. |
| Contact import — header detection, phone normalisation, dedupe | IMPLEMENTED, LOCAL_UNIT_VERIFIED, BROWSER_VERIFIED | Writes outreach_contacts, which already exists. |
| Inbox — conversation list, thread, AI/human handoff | IMPLEMENTED, BROWSER_VERIFIED, BLOCKED_ONLY_BY_SCHEMA | comm_set_conversation_mode() and the two tables the thread is read from. |
| Analytics — delivery, outcome and cost aggregates | IMPLEMENTED, BROWSER_VERIFIED, BLOCKED_ONLY_BY_SCHEMA | The messaging half of every figure reads comm_conversations. The call half reads outreach_sends and works today. |

## The two migrations, unapplied on purpose

- `supabase/migrations/20260912110000_communications_hub.sql`
- `supabase/migrations/20260912110500_communications_functions.sql`

They create 12 tables: `comm_account_trust`, `comm_agent_versions`, `comm_agents`, `comm_channel_accounts`, `comm_conversations`, `comm_extractions`, `comm_messages`, `comm_provider_routes`, `comm_risk_assessments`, `comm_talk_sessions`, `comm_webhook_events`, `comm_whatsapp_templates`.

Applying them is a production schema change and is the owner's decision, not this branch's.
