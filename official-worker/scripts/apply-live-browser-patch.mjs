import fs from 'node:fs';
function patch(path,edits){let s=fs.readFileSync(path,'utf8');for(const[from,to,label]of edits){if(!s.includes(from))throw new Error(`Patch anchor missing: ${label} in ${path}`);s=s.replace(from,to)}fs.writeFileSync(path,s);console.log(`patched ${path}`)}
patch('src/browser/BrowserSession.ts',[["      '[class*=\"captcha\" i]',\n      '[id*=\"captcha\" i]',","      '[class*=\"captcha\" i]',\n      '[id*=\"captcha\" i]',\n      '#capture_gate',\n      'img[src*=\"icaptcha.php\" i]',",'ENREG image CAPTCHA selectors']]);
patch('src/orchestrator/ResearchOrchestrator.ts',[["const TTL = 15 * 60 * 1000;","const TTL = 30 * 60 * 1000;\n\nasync function connectResearchBrowser(): Promise<any> {\n  const bridgeUrl = process.env.BROWSERLESS_TOKEN_BRIDGE_URL || ''; const bridgeKey = process.env.BROWSERLESS_TOKEN_BRIDGE_KEY || '';\n  if (!bridgeUrl || !bridgeKey) throw new Error('remote browser bridge is not configured');\n  const r = await fetch(bridgeUrl, { method: 'POST', headers: { Authorization: `Bearer ${bridgeKey}` }, signal: AbortSignal.timeout(10000) });\n  if (!r.ok) throw new Error(`remote browser bridge unavailable (${r.status})`);\n  const body = await r.json() as any; const token = String(body?.token || '').trim(); if (!token) throw new Error('remote browser credential unavailable');\n  return chromium.connectOverCDP(`wss://production-sfo.browserless.io/chromium/stealth?token=${encodeURIComponent(token)}`, { timeout: 30000 });\n}",'Browserless connector helper'],["browser = browser || (await chromium.launch({ headless: false, args: ['--disable-dev-shm-usage', '--no-sandbox'] }));","browser = browser || (await connectResearchBrowser());",'remote browser launch'],["const ctx = await browser.newContext({ locale: 'ka-GE', acceptDownloads: true, viewport: { width: 1440, height: 1000 } });\n    const page = await ctx.newPage();","const ctx = browser.contexts()[0] || (await browser.newContext({ locale: 'ka-GE', acceptDownloads: true, viewport: { width: 1440, height: 1000 } }));\n    const stalePages = ctx.pages(); for (const oldPage of stalePages) await oldPage.close().catch(() => {});\n    const page = await ctx.newPage(); await page.setViewportSize({ width: 1440, height: 1000 }).catch(() => {});",'Browserless default context'],["await ctx.close().catch(() => {});\n      return { result, keep: false };","for (const p of ctx.pages()) await p.close().catch(() => {});\n      return { result, keep: false };",'keep remote default context alive'],["await ctx.close().catch(() => {});\n      return {\n        result:","for (const p of ctx.pages()) await p.close().catch(() => {});\n      return {\n        result:",'keep remote context on exception'],["await session.ctx.close().catch(() => {});\n    this.sessions.delete(jobId);","for (const p of session.ctx.pages()) await p.close().catch(() => {});\n    this.sessions.delete(jobId);",'resume cleanup'],["await session.ctx.close().catch(() => {});\n    const browser = session.browser;","for (const p of session.ctx.pages()) await p.close().catch(() => {});\n    const browser = session.browser;",'skip cleanup']]);
patch('src/index.ts',[["    humanSessionControls: true,","    humanSessionControls: true,\n    liveInteractiveBrowser: true,\n    browserRuntime: 'browserless-cdp',",'health flags'],["app.get('/research/:id/screenshot', auth, async (req: any, res: any) => {","app.post('/research/:id/live', auth, async (req: any, res: any) => {\n  const s = orchestrator.getSession(req.params.id); if (!s) return res.status(404).json({ error: 'active human session not found' });\n  try { const cdp = await s.ctx.newCDPSession(s.page); const out: any = await cdp.send('Browserless.liveURL', { interactable: true, resizable: true, showBrowserInterface: false, quality: 70, type: 'jpeg', timeout: 900000 }); await cdp.detach().catch(() => {});\n    if (out?.error || !out?.liveURL) return res.status(503).json({ error: 'could not open the live verification browser right now — try again' }); s.expires = Date.now() + 30 * 60 * 1000; return res.json({ liveURL: out.liveURL, expiresAt: new Date(s.expires).toISOString(), source: s.step.type === 'entity' ? s.step.source : s.step.key });\n  } catch (e) { console.error(`[live ${req.params.id}] ${String(e)}`); return res.status(500).json({ error: 'could not open the live verification browser right now — try again' }); }\n});\n\napp.get('/research/:id/screenshot', auth, async (req: any, res: any) => {",'live browser endpoint']]);
if(process.argv.includes('--run')){console.log('starting patched Homatch worker');const{register}=await import('tsx/esm/api');register();await import('../src/index.ts')}
